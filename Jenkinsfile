#! /bin/groovy
import groovy.json.JsonOutput
// Node information
def NODE_IOS = "master"

timeout(time: 30, unit: 'MINUTES') {
    node(NODE_DOCKER_LINUX) {
        try {
            stage("Load Configuration") {
            }

            stage("Post Kintone") {
                // check condition to post kintone notification
                }
            }
        } catch (err) {

        }
    }//node
}//timeout
